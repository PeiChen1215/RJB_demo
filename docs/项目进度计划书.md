# 智学蜂巢 EduHive —— 项目进度计划书

> 赛事：第十五届中国软件杯大赛 A组  
> 赛题：基于大模型的个性化资源生成与学习多智能体系统开发  
> 团队：智学蜂巢项目组  
> 周期：4 周（当前进入决赛前 30 天冲刺阶段）  
> 版本：v1.4（2026-07-03 更新：Command Center 真实数据闭环 + 组件拆分）

---

## 更新说明

截至 **2026-07-03 晚间**：
- 后端补齐 Command Center 所需的全部真实字段：
  - `daily_learning_minutes` / `streak_days` 基于学习事件真实计算。
  - `profile_confidence` 与 `/api/sessions/{id}/profile/evidence` 按维度聚合画像证据。
  - `/api/sessions/{id}/agent-trace` 返回真实 Agent 执行链路。
- `GeneratorAgent` 对缺失 `expected_output` 的练习题自动执行 solution 推导 stdout，支持自动判题。
- 前端 `AgentPanel`、`ProfilePanel` 已接入上述真实数据；`App.tsx` 完成组件拆分（`frontend/src/components/command-center/`）。
- `npm run build` 通过；后端测试 20 passed；C4 Demo 主链路联调通过。
- 端口 8000 占用问题已清理，后端可正常启动。

截至 **2026-06-24 晚间**：
- 后端 Agent 已完成 **10 角色 → 5 执行实体** 分层重构（Orchestrator / Profiler / Navigator / Generator / Reviewer）。
- 决赛所需 **8 张关键数据表** 已落地，资源生成、辩论、代码提交等接口完成持久化闭环。
- **A10 知识熔炉联动** 已落地：代码判题后错误率超阈值自动触发资源重审，生成 `resource_version` 演进记录。
- **A11 Agent 超时熔断** 已落地：单 Agent 调用超过 10 秒自动降级，连续失败 3 次触发 120 秒熔断。
- **A12 主链路集成测试** 已落地：`tests/test_main_chain_integration.py` 覆盖会话 → 聊天 → 资源生成 → 判题触发熔炉 → 行为埋点 → 热力图。
- **C5 认知风格差异化渲染** 已落地：视觉 / 听觉 / 动觉三种模式，听觉型支持浏览器 TTS 朗读。
- **C6 掌握度热力图前端** 已落地：新增「掌握进度」标签页，按 BKT 掌握概率着色展示知识点。
- **C7 学习路径可解释性前端标注** 已落地：ChatPanel 中路径节点悬停显示推荐理由。
- **C8 苏格拉底提问链 UI** 已落地：SocraticPanel 组件支持问题/提示/答案展示与继续引导。
- **C3 PPT 故事线初稿** 与 **C12 7 分钟 Demo 脚本** 已产出。
- 修复 `datetime.utcnow()` 弃用警告，测试告警从 116 条降至 1 条。
- 后端全量测试 **49 passed, 1 skipped**；前端 `npm run build` 通过。
- 代码工程化注释与 TODO 状态清单已覆盖前后端核心脚本。

本版继续明确决赛前剩余重点改进项与冲刺计划。

---

## 一、项目目标

在 4 周内完成一个可运行、可演示、具备国一竞争力的 **Python 个性化学习多智能体系统**。系统以大模型为主推理引擎，以知识图谱为神经符号约束基础，实现对话式画像构建、多 Agent 协同资源生成、辩论议会质量审核、认知风格自适应渲染等核心功能。

---

## 二、技术路线与边界

### 2.1 核心架构

| 层级 | 技术选型 | 备注 |
|------|---------|------|
| 前端 | React 18 + Vite + TypeScript + Tailwind CSS + shadcn/ui | 已集成 Framer Motion、ECharts、Monaco Editor、@tailwindcss/typography |
| 后端 | FastAPI + Python 3.13 | 虚拟环境 `backend/venv` |
| 大模型 | DeepSeek API | 当前使用 DeepSeek Chat/Reasoner；后续可切换讯飞星火 |
| 多智能体 | 自定义 Agent 编排（5 角色执行层） | Orchestrator / Profiler / Navigator / Generator / Reviewer（内部含 Tutor/Evaluator/Debate） |
| 知识图谱 | Neo4j（设计目标）+ MemoryGraph（当前 fallback） | Docker 未安装，当前使用内存图 |
| 数据库 | SQLite（会话、学习记录、资源生成、辩论、代码提交等） | 关键表已补全；决赛前根据运行情况补充索引与迁移脚本 |
| 代码执行 | Pyodide 浏览器沙箱 | 已集成 Monaco Editor |
| 流式通信 | SSE（Server-Sent Events） | 基于 HTTP，浏览器原生支持，已用于聊天和资源生成 |
| 部署 | Docker Compose（待完整验证） | 当前因 Docker 未安装而使用本地启动 |
| 版本控制 | Git + GitHub | `origin` 已切换为 SSH 地址 |

### 2.2 功能边界

| 模块 | 必须完成 | 当前状态 | 决赛前目标 |
|------|---------|---------|-----------|
| 对话式画像 | ✅ 6 维度画像 | ✅ 已完成 | 已补充认知风格行为证据与置信度 |
| 多智能体协同 | ✅ 6 个核心 Agent | ✅ 已完成 5 执行实体分层 | 保留 Reviewer 内部 4-Prompt 辩论 |
| 5 种资源类型 | ✅ 文档/导图/练习/代码案例/音频 | ✅ 文档/导图/练习/代码案例已渲染；音频为文本 | 稳定生成 + 辩论缓存降级（已实现缓存与 fast/full 模式） |
| 学习路径规划 | ✅ A* + BKT | ⚠️ 基础路径规划已完成，BKT 简化版已有 | A* 算法替换当前启发式 + 路径可解释性 |
| 智能辅导 | ✅ 苏格拉底提问链 | ⚠️ 后端 5 阶段提问链已实现，前端联动待做 | 4 轮提问链 + 一键答案 + 答错触发 |
| 学习效果评估 | ✅ 掌握度热力图 | ✅ 热力图已接入真实 BKT 数据 | 思维路径回放 + 趋势预测 |
| 防幻觉 | ✅ 知识图谱约束 + AST 校验 + Guardian | ✅ 三层机制完整 | 补充 3 类可测试 Guardian 用例 |
| 多模态视频 | ⚠️ 用动态图解 + TTS 呈现 | ⚠️ audio_text 已生成，TTS 音频未接入 | 不追求真实视频生成 |

---

## 三、人员分工

| 成员 | 主要职责 | 核心交付物 |
|------|---------|-----------|
| **成员 A（后端主程）** | Agent 业务逻辑、辩论议会、资源生成、防幻觉测试 | `app/agents/` 全部模块、Prompt 工程、单元测试 |
| **成员 B（后端/AI）** | 知识图谱、大模型 API、数据持久化、BKT、知识熔炉 | Neo4j 数据导入、SQLite 模型、API 接口、数据分析 |
| **成员 C（前端/产品）** | 前端界面、Demo 素材、文档视频、交互可视化 | React 组件、PPT、演示视频、部署文档 |

---

## 四、4 周详细计划与实际进度

### Week 1：骨架跑通（第 1-7 天）

**目标**：环境可用，前后端联调通过，知识图谱可查询。

| 天数 | 成员 A | 成员 B | 成员 C |
|------|--------|--------|--------|
| Day 1 | 搭建 FastAPI 项目结构，确认 Agent 接口 | 设计 Neo4j 图谱 Schema / MemoryGraph fallback | 搭建 React 项目，配置 Tailwind |
| Day 2 | 封装 DeepSeek API 调用 | 实现图存储查询接口 | 实现主布局、导航、画像卡片 |
| Day 3 | 设计 Agent 消息协议 | 实现 `/api/graph` 系列接口 | 实现对话界面基础 |
| Day 4 | 实现 Profiler Agent MVP | 实现会话创建/画像接口 | 实现知识图谱可视化（ECharts） |
| Day 5 | 实现 Navigator Agent MVP | 实现学习路径计算接口 | 集成 Pyodide 代码沙箱 |
| Day 6 | 联调 Profiler → Navigator → 前端 | 联调图谱接口 | 联调对话、沙箱、图谱 |
| Day 7 | **里程碑**：单知识点端到端走通 | 完善数据模型 | 整理 UI 组件库 |

**Week 1 验收标准**：
- [x] 前后端能分别启动并联调
- [x] 前端能显示知识图谱
- [x] 用户输入知识点后，系统能返回学习路径
- [x] Pyodide 能运行 Python 代码
- [ ] `docker-compose up` 能启动所有服务（因 Docker 未安装延后）

**实际偏差**：
- 原计划的讯飞星火 API 改为 DeepSeek API，因 DeepSeek 更便于本地调试且支持 OpenAI 兼容接口。
- Docker / Neo4j 因环境未安装，提前启用 MemoryGraph fallback，未阻塞开发。

---

### Week 2：核心资源生成与辩论议会（第 8-14 天）

**目标**：实现 Builder 资源生成 + 4-Agent 辩论议会真实运行。

| 天数 | 成员 A | 成员 B | 成员 C |
|------|--------|--------|--------|
| Day 8 | 实现 Builder Agent，生成 Markdown 文档 | 完善知识点节点属性 | 实现资源展示卡片 |
| Day 9 | 实现练习题自动生成 + 测试用例 | 实现练习数据表 | 实现练习题交互界面 |
| Day 10 | 实现代码实操案例生成 | 代码 AST 校验工具 | 代码运行结果展示 |
| Day 11 | 实现 Expert Agent（技术审核） | 实现 Guardian 安全规则 | 实现辩论过程可视化 |
| Day 12 | 实现 Teacher Agent（教学法审核） | 实现知识图谱一致性校验 | 辩论报告 UI |
| Day 13 | 实现 Student-Sim Agent（可理解性） | 整合三层防幻觉机制 | 资源生成进度展示 |
| Day 14 | **里程碑**：辩论议会可真实运行并输出报告 | 性能基准测试 | 资源渲染优化 |

**Week 2 验收标准**：
- [x] 输入知识点能生成文档、导图、练习、代码案例
- [x] 4 个辩论 Agent 真实调用大模型 API 并投票
- [x] Guardian 能识别明显幻觉/超纲内容
- [x] 辩论报告在前端可查看
- [x] 已修复 Windows 路径反斜杠导致的 AST 误报

**实际偏差**：
- 资源生成与辩论议会实现后，实际可交互性仍较弱，因此 Week 2 后期已提前介入部分 Week 3 的 UI 美化工作。

---

### Week 3：个性化渲染与辅导评估（第 15-21 天）

**目标**：认知风格自适应渲染、苏格拉底辅导、学习评估闭环。

| 天数 | 成员 A | 成员 B | 成员 C |
|------|--------|--------|--------|
| Day 15 | 实现认知风格推断逻辑 | 完善学生画像数据模型 | 实现认知风格切换 UI |
| Day 16 | 实现 6 种渲染策略 | 画像动态更新接口 | 文档/导图/代码/音频渲染组件 |
| Day 17 | 实现 Socrates 辅导 Agent | 错误模式分析服务 | 辅导提问链 UI |
| Day 18 | 实现 Evaluator Agent | 学习行为日志表 | 掌握度热力图 |
| Day 19 | 画像 → 路径 → 资源的闭环联调 | BKT 简化实现 | 学习趋势曲线 |
| Day 20 | 全流程端到端测试 | 修复数据一致性问题 | UI 动效与响应式适配 |
| Day 21 | **里程碑**：完整学习循环跑通 | 压力测试 | 用户体验走查 |

**Week 3 验收标准**：
- [x] 同一资源能以不同认知风格呈现（视觉/听觉/动觉切换 + TTS 朗读已实现）
- [x] 学生答错后触发苏格拉底式辅导（SocraticPanel 已接入，继续引导可进入下一轮）
- [x] 练习结果能更新画像和路径
- [x] 系统能展示薄弱知识点热力图（BKT 数据 + 前端热力图已联调）
- [x] UI 已完成 Command Center 重构，并通过构建验证
- [x] 画像证据、Agent trace、学习时长/连续天数已接入真实数据

**实际偏差**：
- 原计划 Week 3 中后期才做 UI 美化，实际在 Week 2 末就开始全面重构，原因是 Tailwind 样式未生效和前端构建报错集中暴露。
- 修复 Tailwind 问题的关键是补回 `postcss.config.js`。
- TypeScript 构建错误主要是未使用导入和 `framer-motion` 事件类型冲突。
- 实际开发中由后端同学（非原计划中的 B 单独负责）补充了画像证据、Agent trace、学习时长等真实字段，以支持 C 的 Command Center 展示；后续需在文档中明确接口负责人。
- `frontend/src/App.tsx` 单文件过大，于 2026-07-03 提前拆分，属于工程化优化，不影响功能排期。

---

### Week 4：交付物准备与系统优化（第 22-30 天）

**目标**：代码稳定、文档完整、演示出彩。

| 天数 | 成员 A | 成员 B | 成员 C |
|------|--------|--------|--------|
| Day 22 | 辩论议会性能优化（缓存/超时） | Neo4j 查询优化 | 演示脚本编写 |
| Day 23 | Agent 异常处理与熔断 | 后端日志与监控 | PPT 初稿 |
| Day 24 | 全链路 Bug 修复 | SQLite 数据备份 | 演示视频录制 |
| Day 25 | 防幻觉机制强化 | Docker 部署验证 | 视频剪辑 |
| Day 26 | 代码审查与重构 | 接口文档完善 | 系统开发说明书 |
| Day 27 | 集成测试 | 部署文档 | PPT 定稿 |
| Day 28 | 最终联调 | 环境复现测试 | 演示彩排 |
| Day 29 | 打包源码与数据集 | 部署包最终验证 | 提交物整理 |
| Day 30 | **提交日**：最终检查与上传 | 最终检查 | 最终检查 |

**Week 4 验收标准**：
- [ ] 7 分钟演示视频控制在 6 分 30 秒内
- [ ] 演示 PPT 15-20 页，逻辑清晰
- [ ] 系统开发说明书 50+ 页
- [ ] Docker 一键部署在干净环境验证通过
- [ ] 所有代码能稳定运行 30 分钟不崩溃

---

## 五、关键里程碑

| 时间节点 | 里程碑 | 状态 | 验收方式 |
|---------|--------|------|---------|
| Day 7 | 骨架跑通 | ✅ 完成 | 前后端联调通过 |
| Day 14 | 辩论议会真实运行 + 5 角色重构 | ✅ 完成 | 生成资源并输出审核报告；Agent 已收敛为 5 执行实体 |
| Day 21 | 完整学习闭环 | ✅ 完成 | 画像→路径→资源→辩论→代码→辅导→评估已端到端跑通；画像证据/Agent trace/学习时长已真实化 |
| Day 28 | 交付物就绪 | ⏳ 进行中 | PPT + 视频 + 文档 + 部署包 |
| Day 30 | 正式提交 | ⏳ 待开始 | 所有文件打包上传 |

---

## 六、风险与应对（已更新）

| 风险 | 影响 | 概率 | 应对策略 |
|------|------|------|---------|
| 大模型 API 不稳定 | 高 | 中 | 申请足额额度；本地缓存热门资源；准备离线降级模式 |
| Agent 响应时间过长 | 高 | 中 | 辩论议会异步化；单 Agent 10 秒超时；SSE 流式展示进度 |
| Docker / Neo4j 部署受阻 | 中 | 高 | 当前已用 MemoryGraph fallback；答辩前必须在干净环境完成 Docker 验证 |
| 知识图谱数据不完整 | 中 | 中 | 优先覆盖 30 个核心节点；其他节点答辩时说明为扩展预留 |
| Pyodide 加载慢 | 中 | 中 | 使用 CDN；首次加载时显示进度条；准备后端 Docker 沙箱 fallback |
| GitHub 推送网络问题 | 低 | 低 | ✅ 已通过 SSH 成功推送；`origin` 已切换为 SSH 地址 |
| 代码密钥泄露 | 高 | 低 | ✅ 已移除测试文件中的硬编码 DeepSeek API Key，并改写 Git 历史 |
| 前端组件开发超时 | 中 | 低 | ✅ 已用 Bento + Glassmorphism 完成核心界面重构 |
| 演示视频超时 | 低 | 低 | 精确脚本到秒；准备 1.2 倍速剪辑版 |

---

## 七、每日站会检查项

1. 昨天完成了什么？
2. 今天计划做什么？
3. 有什么阻塞需要队友支持？
4. 是否偏离本周里程碑？

---

## 八、提交物清单

| 序号 | 提交项 | 负责人 | 截止时间 | 状态 |
|------|--------|--------|---------|------|
| 1 | 演示 PPT | 成员 C | Day 27 | ⏳ |
| 2 | 项目源码 + 数据集 + 配置文件 | 全员 | Day 29 | ✅ 已推送至 GitHub（SSH） |
| 3 | 7 分钟演示视频 | 成员 C | Day 27 | ⏳ |
| 4 | 系统开发说明书 | 成员 C | Day 27 | ⏳ |
| 5 | Docker 部署包 + 部署文档 | 成员 B | Day 28 | ⏳ |
| 6 | AI Coding 工具说明 | 成员 A | Day 28 | ⏳ |

---

## 九、备注

- 本计划基于 3 人全职开发、每日有效工作 10 小时制定。
- 所有 Agent 的 System Prompt 需在 Week 1 结束前确定初稿。
- 每周五晚进行里程碑评审，必要时调整下周计划。
- 国一竞争核心：**辩论议会的真实运行效果** + **Pyodide 即时运行体验** + **知识图谱约束的技术深度**。
- 前端最新验证截图保存在 `logs/frontend_redesign_*.png`。

---

## 十、实际开发偏差与复盘

### 10.1 与原计划不一致的地方

| 原计划 | 实际情况 | 原因 |
|--------|---------|------|
| 讯飞星火 4.5 Max / Spark Pro | 使用 DeepSeek API | 本地调试更方便，接口兼容 OpenAI SDK |
| 第 1 周完成 Docker 环境 | Docker 未安装，使用 MemoryGraph fallback | 当前机器无 Docker，为不阻塞开发先降级 |
| 第 3 周中后期做 UI 美化 | 第 2 周末即开始全面重构 | Tailwind 样式失效、TS 构建报错集中爆发，必须提前修 |
| HTTPS 推送 GitHub | 改为 SSH 推送 | 当前环境 HTTPS 连 GitHub 被重置，SSH 可用 |
| 测试文件使用占位 key | 测试文件曾硬编码真实 key | 初期为了本地跑通测试，后通过 GitHub Secret Scanning 发现并清理 |

### 10.2 关键踩坑与解决方案

1. **Tailwind 样式只有 4KB，组件无样式**
   - 根因：`frontend/postcss.config.js` 缺失。
   - 解决：补回 `postcss.config.js`（`tailwindcss` + `autoprefixer`），CSS 恢复到 62KB。

2. **`npm run build` TypeScript 报错**
   - 根因：未使用的导入和 `framer-motion` 事件类型冲突。
   - 解决：清理未使用导入；调整 `glass-card.tsx`。

3. **Windows 路径反斜杠导致 AST 安全校验误报**
   - 根因：代码安全过滤器把 Windows 路径中的 `\` 当成转义字符。
   - 解决：在过滤逻辑中对 Windows 路径做归一化/白名单处理。

4. **GitHub 推送被 Secret Scanning 拦截**
   - 根因：`backend/test_*.py` 中硬编码了 DeepSeek API Key。
   - 解决：替换为环境变量占位，并改写 Git 历史。

5. **HTTPS 无法连接 GitHub**
   - 根因：当前网络环境访问 `github.com:443` 被重置。
   - 解决：改用 SSH 推送，并将 `origin` 远程地址切换为 SSH。

---

## 十一、决赛前重点改进项（已确认清单）

以下 13 项为决赛前 30 天必须完成的核心改进，另附 2 项支撑项（BKT、资源版本化）。

### 11.1 架构与工程层（5 项）

| 编号 | 改进项 | 核心思想 | 工作量 | 负责人 | 状态 |
|------|--------|---------|--------|--------|------|
| 1 | **10 Agent → 5 角色分层** | 降低多 Agent 组合爆炸，工程上收敛为 5 个执行实体，Reviewer 内部用 4 个 Prompt 模拟辩论 | 3-4 天 | 成员 A | ✅ 已完成 |
| 2 | **LangGraph 异构扩展预留** | Agent 通过消息总线解耦，文档中说明未来可平滑引入 LangGraph | 半天 | 成员 A | ⏳ 文档待补充 |
| 3 | **响应时间目标改现实** | 首屏 3 秒内返回进度，完整资源 15-30 秒完成，SSE 实时展示阶段 | 1-2 天 | 成员 A | ✅ SSE 进度已实现，待设定明确 SLA |
| 4 | **辩论议会缓存与降级** | 热门资源缓存；普通讲解走快速审核；新知识点/代码题走完整辩论 | 2-3 天 | 成员 A | ✅ 已实现 fast/full 模式 + SQLite 缓存 |
| 5 | **补关键数据表** | 补 `resource`、`generation_task`、`debate_record`、`code_submission`，`users` 补 `password_hash` | 2-3 天 | 成员 B | ✅ 已落地 8 张表 |
| 6 | **防幻觉 3 类可测试** | 不存在 API、语法错误、前置关系冲突三类 Guardian 测试用例 | 2 天 | 成员 A | ⏳ 待补充测试用例 |

### 11.2 个性化与学习科学层（5 项）

| 编号 | 改进项 | 核心思想 | 工作量 | 负责人 | 状态 |
|------|--------|---------|--------|--------|------|
| 7 | **认知风格推断要有证据** | 通过前端埋点（点击导图、运行代码、停留时长等）支撑画像 | 2-3 天 | 成员 B | ✅ 埋点接口、自动证据生成、前端展示均已落地 |
| 8 | **认知风格差异化渲染** | 同一资源按 2-3 种风格（视觉/听觉/动觉）呈现 | 3-4 天 | 成员 C | ✅ 已实现 3 种风格切换 + TTS 朗读 |
| 9 | **掌握度热力图** | 基于 BKT 简化版 + 练习/行为数据展示薄弱点 | 3-4 天 | 成员 B/C | ✅ 后端 BKT + 热力图接口 + 前端热力图展示均已联调 |
| 10 | **学习路径可解释性** | 路径图上标注前置依赖、掌握度、易错点等推荐理由 | 1-2 天 | 成员 B/C | ⚠️ Navigator 已返回 path_explanation / reason；ChatPanel 路径 Tooltip 已实现；图谱节点边标注待完善 |
| 11 | **苏格拉底提问链** | 学生答错后 4 轮引导提问 + 一键答案 | 3-4 天 | 成员 A | ✅ 后端 5 阶段提问链 + 前端 SocraticPanel 联调完成；继续引导上下文已持久化 |

### 11.3 创新亮点层（3 项）

| 编号 | 改进项 | 核心思想 | 工作量 | 负责人 | 状态 |
|------|--------|---------|--------|--------|------|
| 12 | **可解释思维路径回放** | 记录学生输入、代码改动、错误、Agent 干预，生成时间线回放 | 2-3 天 | 成员 C | ⏳ 待设计时间线数据模型与 UI |
| 13 | **知识熔炉（群体资源进化）** | 聚合学生错误模式，触发资源重审，展示 v1.0→v1.2 演进 | 3-4 天 | 成员 B | ⚠️ 错误统计、版本保存、判题后台触发重审已实现；前端 v1.0→v1.2 演进展示待联调 |
| 14 | **全息代码沙箱（变量可视化）** | Pyodide 运行后展示变量名、类型、值 | 1-2 天 | 成员 C | ⚠️ 后端 `/api/code/*` 已返回变量快照；前端变量面板 UI 待完善 |

### 11.4 支撑项（2 项）

| 编号 | 支撑项 | 说明 | 工作量 | 负责人 | 状态 |
|------|--------|------|--------|--------|------|
| 15 | **BKT 简化版** | 掌握度热力图底层，否则退化为练习正确率统计 | 2-3 天 | 成员 B | ✅ 标准 BKT 公式已实现并接入评估接口 |
| 16 | **资源版本化表** | 为知识熔炉提供 `resource_version` 数据支撑 | 0.5-1 天 | 成员 B | ✅ 已创建 |

---

## 十二、决赛前 30 天冲刺计划

### Week 1（第 1-7 天）：地基与主链路稳定

**目标**：Demo 主链路稳定，不再出 500/白屏。

| 负责人 | 任务 |
|--------|------|
| 成员 A | 1. 10 Agent → 5 角色重构<br>2. 响应时间目标改现实（SSE 阶段进度）<br>3. 防幻觉 3 类测试<br>4. 辩论议会缓存与降级 |
| 成员 B | 1. 补关键数据表（resource / generation_task / debate_record / code_submission / learning_event / resource_version）<br>2. `users` 表补 `password_hash`<br>3. BKT 简化版设计 |
| 成员 C | 1. 固定 7 分钟 Demo 路径页面跳转<br>2. UI 响应式/细节收尾<br>3. PPT 故事线初稿 |

**本周验收**：
- [ ] 登录 → 画像 → 路径 → 资源 → 辩论 → 代码 → 辅导 → 报告，主链路 10 分钟能跑完
- [ ] Guardian 3 类测试通过
- [ ] 辩论对新知识点走完整议会，热门资源走缓存

---

### Week 2（第 8-14 天）：个性化与学习闭环

**目标**：画像有证据、路径有解释、答错有辅导、掌握度可视化。

| 负责人 | 任务 |
|--------|------|
| 成员 A | 1. 苏格拉底提问链<br>2. 学习路径可解释性后端支持<br>3. LangGraph 异构扩展文档 |
| 成员 B | 1. 认知风格证据计算（基于 learning_event）<br>2. BKT 简化版实现<br>3. 掌握度热力图数据接口 |
| 成员 C | 1. 认知风格差异化渲染（2-3 种风格）<br>2. 掌握度热力图前端<br>3. 学习路径可解释性前端标注 |

**本周验收**：
- [ ] 答错后触发苏格拉底 4 轮提问
- [ ] 同一资源能切换 2-3 种风格
- [ ] 路径图能显示推荐理由
- [ ] 热力图展示知识点掌握度

---

### Week 3（第 15-23 天）：创新亮点集成与 Demo 打磨

**目标**：所有亮点可演示，7 分钟 Demo 脚本卡死。

| 负责人 | 任务 |
|--------|------|
| 成员 A | 1. 辩论议会与知识熔炉联动（错误率触发重审）<br>2. Agent 异常处理与超时熔断<br>3. 集成测试 |
| 成员 B | 1. 知识熔炉后端（群体错误统计 + 资源版本演进）<br>2. Neo4j 部署验证（如果环境允许）<br>3. 接口文档完善<br>4. 补齐 Command Center 真实数据：画像证据、Agent trace、学习时长/连续天数（✅ 已完成） |
| 成员 C | 1. 可解释思维路径回放时间线（⏳ 待实现）<br>2. 全息代码沙箱变量可视化（⚠️ UI 待完善）<br>3. 知识熔炉前端演进展示（⏳ 待实现）<br>4. 7 分钟 Demo 脚本排练（✅ 已产出）<br>5. `App.tsx` 组件拆分（✅ 已完成） |

**本周验收**：
- [ ] 思维路径回放可拖动查看
- [ ] 代码运行后展示变量面板
- [ ] 知识熔炉能用模拟数据演示 v1.0→v1.2
- [ ] Demo 脚本 7 分钟内完成

---

### Week 4（第 24-30 天）：交付物冲刺

**目标**：提交物齐全，系统稳定运行 30 分钟。

| 负责人 | 任务 |
|--------|------|
| 成员 A | 1. 最终 Bug 修复<br>2. AI Coding 工具说明<br>3. 集成测试与稳定运行 30 分钟验证 |
| 成员 B | 1. Docker 部署包最终验证<br>2. 部署文档<br>3. 环境复现测试 |
| 成员 C | 1. PPT 定稿<br>2. 7 分钟演示视频录制+剪辑<br>3. 系统开发说明书 |

**最终验收**：
- [ ] PPT 15-20 页
- [ ] 视频 6 分 30 秒以内
- [ ] 说明书 50+ 页
- [ ] Docker 一键部署在干净环境验证通过
- [ ] 系统稳定运行 30 分钟
- [ ] GitHub 最终代码 push

---

## 十三、三人每周任务看板（简化版）

| 周次 | 成员 A | 成员 B | 成员 C |
|------|--------|--------|--------|
| **Week 1** | Agent 分层、SSE 阶段进度、防幻觉测试、辩论缓存 | 补数据表、BKT 设计、password_hash | Demo 路径页面、UI 收尾、PPT 故事线 |
| **Week 2** | 苏格拉底提问链、路径解释后端、LangGraph 文档 | 认知风格证据、BKT、热力图接口 | 风格渲染、热力图前端、路径解释前端 |
| **Week 3** | 知识熔炉联动、Agent 熔断、集成测试 | 知识熔炉后端、Neo4j 验证、接口文档 | 思维回放、变量可视化、Demo 排练 |
| **Week 4** | Bug 修复、AI Coding 说明、稳定测试 | Docker 部署、部署文档、环境复现 | PPT、视频、说明书 |
